# Notes 实验矩阵（A/B/C/D）

运行日期：2026-09-12T17:45:11.060Z。这是按 #63 固定协议驱动真实 `node bin/cli.js chat` 的初步 smoke/关键对照结果；每格样本量较小，不能据此下最终结论。**下一步需要 100+ 次才能定论。**

## 协议

- A：`--no-final-guard --no-notes`（仅移除 notes，其他 skill 保留）；B：`--no-final-guard`；C：`--no-final-guard --notes-ledger`；D：默认 provenance gate。
- 每 run 使用独立 transcript、`ERIX_NOTES_DIR` 和 session；提示固定执行一次随机密钥命令、三段 `seq`，最后原样回答第一次密钥。
- “错误具体值” = 重跑冒充 + 编造。区间分母是**完成且可判定 run**；运行失败、模型排除和不可判定记录保留在原始计数中，但不计作无答案，也不进入该 CI 分母。“note 读取率”按发生 `note_list`/`note_read` 的 run 计，“归档读取率”按读取 outputs 目录的 run 计。
- 随机密钥仅在结果 JSON 中保留“前 4 位 + 长度”脱敏摘要，本文不写入明文。

## 矩阵结果

| 臂 | 模型 | n | 完成 | 可判定 | 运行失败 | 命中首次 | 重跑冒充 | 编造 | 无答案 | note 读取率 | 归档读取率 | fail-closed | 错误具体值比例（Wilson 95%） |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A | kimi-for-coding | 3 | 3 | 3 | 0 | 1 | 0 | 0 | 2 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| A | k3 | 3 | 3 | 3 | 0 | 0 | 0 | 0 | 3 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| B | kimi-for-coding | 3 | 3 | 3 | 0 | 1 | 0 | 0 | 2 | 0.0% | 66.7% | 0 | 0.0% [0.0%, 56.1%] |
| B | k3 | 3 | 3 | 3 | 0 | 0 | 0 | 0 | 3 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| C | kimi-for-coding | 3 | 3 | 3 | 0 | 0 | 0 | 0 | 3 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| C | k3 | 3 | 3 | 3 | 0 | 0 | 0 | 0 | 3 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| D | kimi-for-coding | 3 | 0 | 0 | 3 | 0 | 0 | 0 | 0 | 0.0% | 100.0% | 3 | — [—, —] |
| D | k3 | 3 | 0 | 0 | 3 | 0 | 0 | 0 | 0 | 0.0% | 66.7% | 3 | — [—, —] |

## 初步结论

本次还存在运行失败（D/kimi-for-coding 3/3、D/k3 3/3）；relay 拒绝、模型排除或超时不应解读为模型行为结果。 各格并列呈现：A/kimi-for-coding=0.0% (可判定 3，命中首次 1，无答案 2)；A/k3=0.0% (可判定 3，命中首次 0，无答案 3)；B/kimi-for-coding=0.0% (可判定 3，命中首次 1，无答案 2)；B/k3=0.0% (可判定 3，命中首次 0，无答案 3)；C/kimi-for-coding=0.0% (可判定 3，命中首次 0，无答案 3)；C/k3=0.0% (可判定 3，命中首次 0，无答案 3)。样本量不足以排序臂或模型，Wilson 区间较宽且可能重叠；失败/排除格也不具可比性，应继续做 100+ 次、交错运行、分模型报告后再评估。

本次总耗时约 2100.3 秒（脚本 wall clock 以运行日志为准）。

## 方法与混杂因素

- 采用固定种子 `notes-experiment-2026-09-13` 的轮转顺序，避免先跑完某一模型/臂造成时间与服务状态混杂；样本量仍不足以估计稳定效应。
- `kimi-for-coding` 与 `k3` 是修复后重跑的目标模型；旧数据中的 deepseek relay 拒绝保留作历史记录并排除出修复后比较，不应解释为模型行为。
- 臂之间同时改变 notes、final guard 和 skills 配置；模型服务负载、上下文压缩、提示协议、工作目录及运行时序都可能混杂。该实验是验证性 smoke/关键对照，不是随机化因果试验。
- 原始计数和失败计数保留；仅完成且可判定记录用于错误比例和 Wilson 区间。敏感值只写入前缀和长度。

## 修复后重跑

# Notes 实验矩阵（A/B/C/D）

运行日期：2026-09-12T17:45:11.060Z。这是按 #63 固定协议驱动真实 `node bin/cli.js chat` 的初步 smoke/关键对照结果；每格样本量较小，不能据此下最终结论。**下一步需要 100+ 次才能定论。**

## 协议

- A：`--no-final-guard --no-notes`（仅移除 notes，其他 skill 保留）；B：`--no-final-guard`；C：`--no-final-guard --notes-ledger`；D：默认 provenance gate。
- 每 run 使用独立 transcript、`ERIX_NOTES_DIR` 和 session；提示固定执行一次随机密钥命令、三段 `seq`，最后原样回答第一次密钥。
- “错误具体值” = 重跑冒充 + 编造。区间分母是**完成且可判定 run**；运行失败、模型排除和不可判定记录保留在原始计数中，但不计作无答案，也不进入该 CI 分母。“note 读取率”按发生 `note_list`/`note_read` 的 run 计，“归档读取率”按读取 outputs 目录的 run 计。
- 随机密钥仅在结果 JSON 中保留“前 4 位 + 长度”脱敏摘要，本文不写入明文。

## 矩阵结果

| 臂 | 模型 | n | 完成 | 可判定 | 运行失败 | 命中首次 | 重跑冒充 | 编造 | 无答案 | note 读取率 | 归档读取率 | fail-closed | 错误具体值比例（Wilson 95%） |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A | kimi-for-coding | 3 | 3 | 3 | 0 | 1 | 0 | 0 | 2 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| A | k3 | 3 | 3 | 3 | 0 | 0 | 0 | 0 | 3 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| B | kimi-for-coding | 3 | 3 | 3 | 0 | 1 | 0 | 0 | 2 | 0.0% | 66.7% | 0 | 0.0% [0.0%, 56.1%] |
| B | k3 | 3 | 3 | 3 | 0 | 0 | 0 | 0 | 3 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| C | kimi-for-coding | 3 | 3 | 3 | 0 | 0 | 0 | 0 | 3 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| C | k3 | 3 | 3 | 3 | 0 | 0 | 0 | 0 | 3 | 0.0% | 100.0% | 0 | 0.0% [0.0%, 56.1%] |
| D | kimi-for-coding | 3 | 0 | 0 | 3 | 0 | 0 | 0 | 0 | 0.0% | 100.0% | 3 | — [—, —] |
| D | k3 | 3 | 0 | 0 | 3 | 0 | 0 | 0 | 0 | 0.0% | 66.7% | 3 | — [—, —] |

## 初步结论

本次还存在运行失败（D/kimi-for-coding 3/3、D/k3 3/3）；relay 拒绝、模型排除或超时不应解读为模型行为结果。 各格并列呈现：A/kimi-for-coding=0.0% (可判定 3，命中首次 1，无答案 2)；A/k3=0.0% (可判定 3，命中首次 0，无答案 3)；B/kimi-for-coding=0.0% (可判定 3，命中首次 1，无答案 2)；B/k3=0.0% (可判定 3，命中首次 0，无答案 3)；C/kimi-for-coding=0.0% (可判定 3，命中首次 0，无答案 3)；C/k3=0.0% (可判定 3，命中首次 0，无答案 3)。样本量不足以排序臂或模型，Wilson 区间较宽且可能重叠；失败/排除格也不具可比性，应继续做 100+ 次、交错运行、分模型报告后再评估。

本次总耗时约 2100.3 秒（脚本 wall clock 以运行日志为准）。

## 方法与混杂因素

- 采用固定种子 `notes-experiment-2026-09-13` 的轮转顺序，避免先跑完某一模型/臂造成时间与服务状态混杂；样本量仍不足以估计稳定效应。
- `kimi-for-coding` 与 `k3` 是修复后重跑的目标模型；旧数据中的 deepseek relay 拒绝保留作历史记录并排除出修复后比较，不应解释为模型行为。
- 臂之间同时改变 notes、final guard 和 skills 配置；模型服务负载、上下文压缩、提示协议、工作目录及运行时序都可能混杂。该实验是验证性 smoke/关键对照，不是随机化因果试验。
- 原始计数和失败计数保留；仅完成且可判定记录用于错误比例和 Wilson 区间。敏感值只写入前缀和长度。


## 修复前旧数据（保留；分类 bug 影响结论）

旧矩阵仍完整保存在 `scripts/notes-experiment-results.json` 的 `legacy` 字段；旧分类把终稿抽取值混入 generatedValues，可能系统性低估 invented，不能与修复后分类直接比较。
